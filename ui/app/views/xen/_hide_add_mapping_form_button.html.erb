<% form_remote_tag(:html => {:id=>"#{host.to_label}_toggle_add_mapping_form_button",
          :style => "display: inline;",
          :title => 'Cancel',
          :action => { :action => "hide_add_mapping_form", :host_id => host.id}},
        :url => url_for({ :action => "hide_add_mapping_form",:host_id => host.id})) do %>
  <%= image_submit_tag "/images/silk/arrow_undo.png", :alt => 'Cancel' %>
<% end %>
